import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

// main.jsx is the Vite entry point that mounts the React UI.
ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
